using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Bullet_Con : MonoBehaviour
{
    public Rigidbody rb;
    public int speed = 20;
    

    // Start is called before the first frame update
    void Start()
    {
        

        //rb.velocity = new Vector3(EnemyPosX, EnemyPosY, EnemyPosZ);
         
    }

}
